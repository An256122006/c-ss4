#include <stdio.h>
int main()
{
	int a;
	printf ("moi nhap gia tri :");
	scanf ("%d",&a);
	if (a%2==0)
	{
		printf ("la so chan"); 
	 } else {
	 	printf ("la so le"); 
	 } 
	 return 0; 
 } 
